# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: ['*']
  push:
    branches: ['*']
    tags: [v*, v*]

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
  SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
  PGP_SECRET: ${{ secrets.PGP_SECRET }}

jobs:
  build:
    name: Build and Test
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.4, 2.13.5, 3.0.0-M3, 3.0.0-RC1]
        java: [adopt@1.8, adopt@1.11]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v10
        with:
          java-version: ${{ matrix.java }}

      - name: Cache sbt
        uses: actions/cache@v2
        with:
          path: |
            ~/.sbt
            ~/.ivy2/cache
            ~/.coursier/cache/v1
            ~/.cache/coursier/v1
            ~/AppData/Local/Coursier/Cache/v1
            ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-cache-v2-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Check that workflows are up to date
        run: csbt ++${{ matrix.scala }} githubWorkflowCheck

      - name: Test
        run: csbt ++${{ matrix.scala }} test

      - name: Binary Compatibility Check
        run: csbt ++${{ matrix.scala }} mimaReportBinaryIssues

      - name: Compress target directories
        run: tar cf targets.tar testing/jvm/target noop/jvm/target docs/target target core/js/target testing/js/target noop/js/target core/jvm/target slf4j/target project/target

      - name: Upload target directories
        uses: actions/upload-artifact@v2
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}
          path: targets.tar

  publish:
    name: Publish Artifacts
    needs: [build]
    if: github.event_name != 'pull_request' && (startsWith(github.ref, 'refs/tags/v'))
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.4]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v10
        with:
          java-version: ${{ matrix.java }}

      - name: Cache sbt
        uses: actions/cache@v2
        with:
          path: |
            ~/.sbt
            ~/.ivy2/cache
            ~/.coursier/cache/v1
            ~/.cache/coursier/v1
            ~/AppData/Local/Coursier/Cache/v1
            ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-cache-v2-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Download target directories (2.13.4)
        uses: actions/download-artifact@v2
        with:
          name: target-${{ matrix.os }}-2.13.4-${{ matrix.java }}

      - name: Inflate target directories (2.13.4)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.12.13)
        uses: actions/download-artifact@v2
        with:
          name: target-${{ matrix.os }}-2.12.13-${{ matrix.java }}

      - name: Inflate target directories (2.12.13)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.0.0-M3)
        uses: actions/download-artifact@v2
        with:
          name: target-${{ matrix.os }}-3.0.0-M3-${{ matrix.java }}

      - name: Inflate target directories (3.0.0-M3)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.0.0-RC1)
        uses: actions/download-artifact@v2
        with:
          name: target-${{ matrix.os }}-3.0.0-RC1-${{ matrix.java }}

      - name: Inflate target directories (3.0.0-RC1)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Import signing key
        run: echo $PGP_SECRET | base64 -d | gpg --import

      - run: csbt ++${{ matrix.scala }} release

      - if: matrix.scala == '2.13.5'
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 2.6

      - if: matrix.scala == '2.13.5'
        run: gem update --system

      - if: matrix.scala == '2.13.5'
        run: gem install sass

      - if: matrix.scala == '2.13.5'
        run: gem install jekyll -v 4

      - if: matrix.scala == '2.13.5'
        run: csbt ++${{ matrix.scala }} docs/publishMicrosite

  scalafmt:
    name: Scalafmt
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.4, 2.13.5]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v10
        with:
          java-version: ${{ matrix.java }}

      - name: Cache sbt
        uses: actions/cache@v2
        with:
          path: |
            ~/.sbt
            ~/.ivy2/cache
            ~/.coursier/cache/v1
            ~/.cache/coursier/v1
            ~/AppData/Local/Coursier/Cache/v1
            ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-cache-v2-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Scalafmt
        run: csbt ++${{ matrix.scala }} scalafmtCheckAll

  microsite:
    name: Microsite
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.5]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v10
        with:
          java-version: ${{ matrix.java }}

      - name: Cache sbt
        uses: actions/cache@v2
        with:
          path: |
            ~/.sbt
            ~/.ivy2/cache
            ~/.coursier/cache/v1
            ~/.cache/coursier/v1
            ~/AppData/Local/Coursier/Cache/v1
            ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-cache-v2-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: 2.6

      - run: gem update --system

      - run: gem install sass

      - run: gem install jekyll -v 4

      - name: Build the microsite
        run: csbt ++${{ matrix.scala }} docs/makeMicrosite